// src/engine/ConsumableSystem.ts
/*
  DEPRECATED: ConsumableSystem removed.

  Consumable/scroll/potion logic was migrated to weapon-bound
  Q/E skills and the `WeaponSkillSystem` + `Player` state.

  This file is retained (intentionally empty) as a historical
  placeholder so older diffs/refs can remain readable.
  See `src/engine/WeaponSkillSystem.ts` for the active implementation.
*/

export {};
